$gallery = $("div#gallery");
$gallery.append("<%=j render @photos %>");

var $photos = $gallery.children("div.photo");
$photos.animate({ opacity: 1 }, 300);

var offset = $gallery.data("offset");
offset = +offset + <%= Photo::PER_REQUEST %>;
$gallery.data("offset", offset);